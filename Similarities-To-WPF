TODO:: ADD EXAMPLES FOR BOTH i.e. STRING FORMAT, CONVERTER for WPF and PIPES FOR ANGULAR
Component = UserControl in WPF
DataBinding = DataBinding in WPF
Templates = Data/Control templates in WPF
Styles = Styles in WPF
Directive = Behavior in WPF
Router-Outlet = ContentControl in WPF
Pipes = StringFormat in WPF, A custom pipe can also work like a Converter in WPF where in we can apply custom logic in order to convert the values
